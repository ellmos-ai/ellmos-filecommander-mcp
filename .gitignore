# Dependencies
node_modules/

# Build output
dist/

# IDE & Tools
.vscode/
.idea/
.claude/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Environment
.env
.env.local
.env.*
!.env.example
!.env.sample
.npmrc
.pypirc

# Secrets / credentials
*.key
*.pem
*.p12
*.pfx
*.crt
*.cert
*.csr
*.token
*.secret
credentials.json
secrets.json
id_rsa*
id_ed25519*
id_ecdsa*
id_dsa*
*_recovery_codes.txt
*recovery*codes*.txt

# Logs
*.log
npm-debug.log*

# Internal development files
_Spielwiese/
*-protocoll.txt
build.py.wip
BUILD_SKILL.md
COMPLETE_PROJECT_CONTEXT.txt
Ideen.md

# mcp-publisher login artifacts (credentials, never commit)
.mcpregistry_*

# lock system (multi-agent coordination, host-local, never commit)
LOCK*.txt
LOCK.*
LOCK
LOCK.permissions.json
*.lock
!package-lock.json
uv.lock

# Multi-host sync conflict copies
*.sync-conflict-*
*.conflict
*-CONFLIT-*
*-conflict-*
*-ASUS-GEI.*
*-WORKSTATION-LG.*
*-WORKSTATION.*
* (kopie)*
* (copy)*

# Temporary files, patch residues & editor backups
*.tmp
*.bak
*.swp
*.orig
*.rej
*~

# Test & coverage caches
.pytest_cache/
.ruff_cache/
.coverage
.coverage.*
coverage/
htmlcov/
.wheel-smoke/

# Lokale Ablage verworfener Artefakte
_archive/

